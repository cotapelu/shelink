"use client";import { SealRowCode } from "./seal-row-code";import { SealRowType } from "./seal-row-type";import { SealRowRequester } from "./seal-row-requester";import { SealRowMatter } from "./seal-row-matter";import { SealRowPurpose } from "./seal-row-purpose";import { SealRowStatus } from "./seal-row-status";import { SealRowDate } from "./seal-row-date";import { SealRowActions } from "./seal-row-actions";import type { SealRequestRow } from "./seal-types";export function SealRow({ row, currentUser, canApprove, onAction }: { row: SealRequestRow; currentUser: { id: string; role: string }; canApprove: boolean; onAction: (action: "detail" | "approve" | "reject" | "stamp" | "cancel") => void; }) { return ( <tr className="ll-row border-t border-border"> <SealRowCode code={row.code} onDetail={() => onAction("detail")} /> <SealRowType type={row.sealType} /> <SealRowRequester name={row.requestedBy.name} /> <SealRowMatter matter={row.matter} /> <SealRowPurpose purpose={row.purpose} /> <SealRowStatus status={row.status} /> <SealRowDate date={row.requestedAt} /> <td className="px-3 py-2 text-right"><SealRowActions row={row} currentUser={currentUser} canApprove={canApprove} onAction={onAction} /></td> </tr> );}
